import { MenuBtnDirective } from './menu-btn.directive';

describe('MenuBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuBtnDirective();
    expect(directive).toBeTruthy();
  });
});
